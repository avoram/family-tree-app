# Member search — Progress

Search within the selected family tree (name match).

---

## Completed

- Search field on tree visualization toolbar
- Match by first name, last name, or full name (case-insensitive)
- Match chips list + highlight in tree
- Auto-expand ancestor branches so matches are visible
- Click match opens member detail via existing `memberSelected` flow
- Helpers: `memberMatchesQuery()`, `findExpandIdsForMember()`
- Docs: README + progress.md updated

---

## Pending

- E2E coverage for search flow
- Redeploy to GitHub Pages
